<UserControl x:Class="NRI.Controls.OrganizerWindowControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:NRI.Controls"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">

    <Grid>
        <local:BaseWindowControl x:Name="BaseControl">
            <local:BaseWindowControl.SideMenuContent>
                <StackPanel>
                    <Button Content="Сохранить" Click="Save_Click" Margin="10" Style="{StaticResource MenuButtonStyle}"/>
                    <Button Content="Удалить" Click="Delete_Click" Margin="10" Style="{StaticResource MenuButtonStyle}"/>
                </StackPanel>
            </local:BaseWindowControl.SideMenuContent>
        </local:BaseWindowControl>
    </Grid>
</UserControl>
